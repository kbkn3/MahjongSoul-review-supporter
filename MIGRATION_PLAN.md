# Node.js 20+移行計画（TDDアプローチ）

## 現状分析

### 技術的な課題
- Node.js 16（Volta）/18（mise.toml）を使用中
- Webpack 4（古いビルドシステム）
- テストフレームワークが未実装
- 多くの依存関係が古い

### ビジネスロジックの複雑性
- 雀魂形式→天鳳形式への変換ロジック
- 点数計算（段位戦、Mリーグ等のルール対応）
- 統計計算（和了率、放銃率等）

## TDDに基づく移行戦略

### フェーズ1: テスト環境の構築とコアロジックのテスト作成

#### 1. Vitestの導入
- モダンで高速なテストランナー
- Viteとの相性が良い
- Vue 3のサポートが充実

#### 2. 最初に書くべきテスト（Red フェーズ）
```
tests/unit/
├── scoring.spec.ts         # 点数計算ロジック
├── dataConversion.spec.ts  # データ変換ロジック（soul2naga等）
└── gameState.spec.ts       # ゲーム状態管理
```

#### 3. テスト対象の優先順位
1. **`soul2naga()` 関数** - 最も複雑な変換ロジック
   - 雀魂の牌譜データを天鳳形式に変換
   - 様々なエッジケースが存在
   
2. **点数計算関数群** - ルールごとの差異
   - 段位戦ルール
   - Mリーグルール
   - 四麻/三麻の違い
   
3. **統計計算** - ビジネス価値が高い
   - 和了率、放銃率、平均順位
   - ルールセット別の計算

### フェーズ2: ビルドシステムの移行

#### 1. Viteへの移行
- **開発体験の大幅な改善**（10-100倍の高速化）
- HMRの改善
- モダンなプラグインエコシステム

#### 2. CRXJS Vite Pluginの導入
- 拡張機能専用の機能
- 自動リロード機能
- Manifest V3の完全サポート

#### 3. 設定ファイルの移行
```
webpack.dev.js  → vite.config.ts
webpack.prod.js → 統合してビルドモード設定
extension-reloader.js → CRXJS で置き換え
```

### フェーズ3: TypeScriptの段階的導入

#### 1. 型定義から開始
- 雀魂のデータ構造の型定義
- メッセージパッシングの型定義
- Chrome API の型定義

#### 2. コンポーネントの移行
- Composition API + `<script setup>`への移行
- Props/Emitsの型付け
- 既存のOptions APIから段階的に移行

### フェーズ4: Vue 3の最適化

#### 1. Composition APIへの移行
- ロジックの再利用性向上
- より良いTypeScriptサポート
- コンポーザブルの作成

#### 2. 状態管理の改善
- Piniaの導入検討
- リアクティビティの最適化
- Chrome Storage APIとの連携改善

### フェーズ5: CI/CDとE2Eテスト

#### 1. GitHub Actionsの設定
- 自動テスト実行
- ビルドチェック
- Node.js 20でのテスト実行

#### 2. Playwrightによる拡張機能のE2Eテスト
- 主要なユーザーフローのテスト
- 実際の雀魂サイトでの動作確認
- 拡張機能のロードと基本動作確認

## 実装の進め方（TDDサイクル）

### 各フェーズでのTDDサイクル
1. **Red**: 失敗するテストを書く
2. **Green**: テストを通す最小限の実装
3. **Refactor**: コードの改善
4. **小さなコミット**: 各サイクルでコミット

### ブランチ戦略
- `feature/phase1-testing` - テスト環境構築
- `feature/phase2-vite-migration` - ビルドシステム移行
- `feature/phase3-typescript` - TypeScript導入
- `feature/phase4-vue3-optimization` - Vue 3最適化
- `feature/phase5-ci-e2e` - CI/CDとE2Eテスト

## 期待される効果

### 開発効率の向上
- **ビルド速度**: Viteによる10-100倍の高速化
- **開発体験**: HMRによる即座のフィードバック
- **デバッグ**: 型チェックによる早期エラー発見

### コード品質の向上
- **TDD**: バグの早期発見と仕様の明確化
- **TypeScript**: 型安全性による実行時エラーの削減
- **自動テスト**: 回帰バグの防止

### 保守性の向上
- **モダンな技術スタック**: 長期的なメンテナンス性
- **明確な型定義**: 新規開発者のオンボーディング向上
- **テストカバレッジ**: 安全なリファクタリング

## 注意事項

- 各フェーズは段階的に実施し、動作確認を怠らない
- 既存機能の動作を保証するためのテストを優先
- ユーザーに影響を与えない範囲での移行を心がける
- パフォーマンス劣化がないことを確認

## 成功指標

- [ ] 全テストがパス
- [ ] ビルド時間の大幅短縮
- [ ] Node.js 20での動作確認
- [ ] 既存機能の完全な動作
- [ ] 新機能開発の効率向上